# SmartResume — One Click AI Resume Generator

# Requirements
streamlit
google-generativeai
python-dotenv

## 🚀 How to Run

1. Install Python 3.10+
2. Run:
   pip install -r requirements.txt
3. Add your Gemini API key in `.env`
4. Start app:
   streamlit run app.py

